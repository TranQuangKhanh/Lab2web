{
  "name": "mini-jsx-framework",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "dev": "./node_modules/.bin/vite",
    "build": "ls -la node_modules/.bin && ./node_modules/.bin/vite build",
    "start": "node ./server.js",
    "preview": "./node_modules/.bin/vite preview",
    "vercel-build": "npm install && ls -la node_modules/.bin && ./node_modules/.bin/vite build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serve-static": "^1.15.0",
    "vite": "^5.4.0",
    "typescript": "^5.0.2"
  }
}
