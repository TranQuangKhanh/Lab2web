{
  "version": 2,
  "buildCommand": "cd ../jsx-without-react && npm install && npm run build",
  "outputDirectory": "jsx-without-react/dist",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
